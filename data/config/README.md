# [DATA_TAG] KEEP | CORE | 配置目录索引

日期：2026-03-08  
执行者：Codex

## 目录作用
- 存放 `pathlearn_app` 与 `pathlearn_eval` 配置文件。
- 论文主线使用 `p2_astar` 相关配置。

## 建议引用优先级
1. `pathlearn_app.conf`：主程序演示运行配置。
2. `pathlearn_eval_p2_astar_baseline.conf`：A* 基线。
3. `pathlearn_eval_p2_astar_heuristic.conf`：A* + NN 启发。
4. `pathlearn_eval_p2_astar_learning.conf`：学习轮实验。

## 历史配置
- `p0/p1` 配置保留作过程追溯，不作为当前论文主结果配置。
